@model TechJobs6Persistent.ViewModels.JobDetailViewModel

<h1>More information for @Model.JobName</h1>

<table class="table">
    <tr>
        <th>Employer</th>
        <td>@Model.EmployerName</td>
    </tr>
    <tr>
        <th>Skills</th>
        <td>@Model.SkillText</td>
    </tr>

</table>

<a asp-controller="Skill" asp-action="AddJob" asp-route-id="@Model.JobId">Add additional skills</a>
